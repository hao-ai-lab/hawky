import Foundation

enum GatewayDefaults {
    /// The default Hawky gateway. Used as the app-wide fallback and as the
    /// "Default" choice in onboarding's Hawky Connection step.
    static let urlString = "https://hawky.hawky.live"
}
